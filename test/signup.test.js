describe('tests de sumar', () => {
  test('1 + 1 debe dar 2', () => {
    let x = 1 + 1;
    expect(x).toEqual(2);
  });

  test('1 + 2 debe dar 3', () => {
    let x = 1 + 2;
    expect(x).toEqual(3);
  });
});
